{
    "version": "0.2.0",
    "configurations": [
        {
            /* GDB must in be in the PATH environment */
            "name": "(Windows) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": []
        },
        {
            "name": "(Apple) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "cwd": "${fileDirname}"
        }
    ]
}